# Sistema de Controle de Projeto - Dependências
# Python 3.10+

# Webview desktop
pywebview>=5.0.0

# Backend web
Flask>=3.0.0

# Banco de dados (sqlite3 já vem no Python, mas garante driver)
# (não é necessário pip install)

# Utilitários
python-dateutil>=2.8.2

# PDF
xhtml2pdf>=0.2.16

# Automação do Outlook no Windows (win32com) — envio de e-mail sem senha
# Só é instalado em Windows; sem este pacote o Outlook não é detectado.
pywin32>=306; sys_platform == "win32"
